//! "/categories/:categoryId/products" or "/categories/:categoryId" : to show products for a specific category.

import React from 'react'

const ProductsByCategory: React.FC = () => {
  return (
    <div>
      
    </div>
  )
}

export default ProductsByCategory
